import 'package:flutter/material.dart';

class ResumeListSection extends StatelessWidget {
  const ResumeListSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
